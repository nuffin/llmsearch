from .base import BaseAgent


class GeneralAgent(BaseAgent):
    """
    General agent, can perform simple tasks, like generating or converting
        that LLM backend can perform directly.
    """

    pass
